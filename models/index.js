// ---------------------------------------------------------------------------
// Dependencies

// Imports the models for the User and Thought for an easier export
const User = require('./User');
const Thought = require('./Thought');

// Export the models
module.exports = { User, Thought };